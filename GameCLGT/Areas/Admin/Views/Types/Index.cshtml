@model IEnumerable<CLGTgame.Areas.Admin.Models.Type>

@{
    ViewBag.Title = "Index";
}
@using PagedList.Mvc;
@using PagedList;

@using (Html.BeginForm())
{
    <div class="row">
        <div class="col-lg-6">
            <div class="input-group">
                <input type="text" class="form-control" name="search" id="search" placeholder="Search...">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit"><div class="glyphicon glyphicon-search"></div></button>
                </span>
            </div>
        </div>
    </div>
}

<p class="btn btn-default">
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type_image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.List_type_game.ID_game)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type_name)
        </td>
        <td>
            <img src="@Html.DisplayFor(modelItem => item.Type_image)" width="30"/>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.List_type_game.ID_game)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
@Html.PagedListPager((IPagedList)ViewBag.Content, page => Url.Action("Index", new { page }))
